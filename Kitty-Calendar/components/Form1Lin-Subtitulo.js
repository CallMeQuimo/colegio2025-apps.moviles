import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Form1Lin-Subtitulo = (titulo = "Título por defecto") => {
  return (
    <View>
      <Text>{titulo}</Text>
    </View>
  )
}

export default Form1Lin-Subtitulo

const styles = StyleSheet.create({

})